{
  "private": true,
  "scripts": {
    "dev":      "docker compose -f docker-compose.yml -f docker-compose.dev.yml up db api app landing admin",
    "dev:down": "docker compose -f docker-compose.yml -f docker-compose.dev.yml down",
    "dev:logs": "docker compose -f docker-compose.yml -f docker-compose.dev.yml logs -f api app",
    "prod":     "docker compose up",
    "prod:down": "docker compose down"
  }
}
